{{#with navigation}}
<div class="nav nav-tabs">
    {{#each links}}
    <div class="flex-column">
        <div class="nav-item">
            <a href="{{href}}" class="nav-link">{{text}}</a>
        </div>
    </div>
    {{/each}}
    <a href="{{href}}" class="nav-link disabled">Disabled</a>
</div>
{{/with}}