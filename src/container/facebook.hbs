<main>
    <section>
        <div>
            <div class="mb-5">
                <h1>{{name}}</h1>
            </div>

            <div class="mb-5">
                <button type="button" class="btn btn-primary btn-lg col-3 me-3">Enter</button>
                <button type="button" class="btn btn-secondary btn-lg col-3">Registration</button>
            </div>
            <hr>
        </div>

        <div>
            {{> title data="Users:"}}
            <ul>

                {{#with users.[0]}}
                {{> facebook}}
                {{/with}}
                <hr>

                {{#with users.[1]}}
                {{> facebook}}
                {{/with}}
            </ul>
        </div>
    </section>
</main>